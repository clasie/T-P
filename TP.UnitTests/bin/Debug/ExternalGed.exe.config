<?xml version="1.0" encoding="utf-8" ?>
<configuration>
    <startup> 
        <supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.7" />
    </startup>
  <appSettings>
    <add key="EncryptedWebServiceGedLogin" value="lDYnQvoEm7Ijjl8WQ+ymAQ=="/>
    <add key="EncryptedWebServiceGedPassWord" value="WC4dfPA5BoE="/>
    <add key="UrlWebServiceGed" value = "http://35.241.194.111/GEDServerV2WS/WebService/document"/>
    
    <!-- 
        ##############################
        # Arguments attendus         #
        ##############################
        arg[0] string codeBare.
        arg[1] string type de fichier a downloader. Entrer UNIQUEMENT FAC ou AFAC. FAC représente une facture, AFAC une annexe.
        
        ##############################
        # Gestion des erreurs retour #
        ##############################
        
        0 == Pas d'erreurs, retour executable.
        
        Gestion des erreurs retour liées aux paramètres
        ###############################################
        
        1 == Erreur lecture du config, retour executable.
        2 == Arguments passés manquent.
        3 == Nombre d'arguments passés non correct.
        
        Gestion des erreurs retour liées aux credentials
        ################################################     
        
        4 == GEDInvalidCredentialsException
        5 == GEDUserLockedTooMuchFailingAttempsToConnectException
        6 == GEDConnectionUnknownException
        7 == GEDUploadFileException
        8 == GEDUploadFileUnknownException
        9 == GEDGlobalUnknownException
        10 == Le type de fichier (arg[0] n'est ni FAC ni AFAC
        
        
    -->
    
  </appSettings>
</configuration>
